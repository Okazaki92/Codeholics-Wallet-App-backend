const { Transaction } = require("../models/transaction");

const getTransactions = async (query) => {
    
}

const addTransaction = () => {
    
}

module.exports = {
    getTransactions,
    addTransaction,
};